[project]
name = "foo"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "ops>=2.10",
    "psycopg2>=2.9.11; python_version < '3.9'",
    "psycopg2>=2.9.11; python_version >= '3.9'",
]



[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
